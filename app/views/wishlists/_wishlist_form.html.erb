<%= form_for(@wishlist_movie, { :url => ''}) do |f| %>
  <% if @wishlist_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wishlist_movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @wishlist_movie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
